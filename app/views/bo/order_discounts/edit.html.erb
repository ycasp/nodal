<% content_for :bo_main_content do %>

<div class="bo-page-header">
  <div class="d-flex justify-content-between align-items-center">
    <div class="d-flex align-items-center gap-3">
      <%= link_to bo_pricing_path(params[:org_slug], tab: 'order_tiers'), class: "text-muted text-decoration-none" do %>
        <i class="fa-solid fa-arrow-left fa-lg"></i>
      <% end %>

      <div class="rounded-circle bg-primary d-flex align-items-center justify-content-center text-white fw-bold" style="width: 60px; height: 60px; font-size: 1.25rem;">
        <i class="fa-solid fa-cart-shopping"></i>
      </div>

      <div>
        <h1><%= t('bo.pricing.order_tiers.edit.title') %></h1>
        <span class="text-muted"><%= t('bo.pricing.order_tiers.edit.subtitle') %></span>
      </div>
    </div>

    <div class="bo-page-actions">
      <%= link_to bo_pricing_path(params[:org_slug], tab: 'order_tiers'), class: "btn btn-outline-secondary" do %>
        <%= t('bo.common.actions.cancel') %>
      <% end %>
      <button type="submit" form="order-discount-form" class="btn btn-primary">
        <i class="fa-solid fa-check"></i> <%= t('bo.pricing.order_tiers.edit.submit') %>
      </button>
    </div>
  </div>
</div>

<div class="bo-page-body">

<% if @discount.errors.any? %>
  <div class="alert alert-danger mb-4">
    <h6 class="alert-heading"><%= t('bo.pricing.errors.title') %></h6>
    <ul class="mb-0">
      <% @discount.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: @discount, url: bo_order_discount_path(params[:org_slug], @discount), method: :patch, id: "order-discount-form" do |f| %>
  <div class="row g-3">
    <div class="col-md-8">
      <div class="card mb-3">
        <div class="card-body">
          <h5 class="card-title mb-4"><%= t('bo.pricing.order_tiers.card_title') %></h5>

          <div class="row g-3">
            <div class="col-md-12">
              <label class="form-label"><%= t('bo.pricing.form.min_order_amount') %> <span class="text-danger">*</span></label>
              <%= f.number_field :min_order_amount, class: "form-control", placeholder: "100.00", step: 0.01, min: 0, required: true, value: @discount.min_order_amount.to_f %>
              <small class="text-muted"><%= t('bo.pricing.form.min_order_hint') %></small>
            </div>
          </div>

          <div class="row g-3 mt-1">
            <div class="col-md-6">
              <label class="form-label"><%= t('bo.pricing.form.discount_type') %> <span class="text-danger">*</span></label>
              <%= f.select :discount_type, [[t('bo.pricing.form.percentage'), 'percentage'], [t('bo.pricing.form.fixed_amount'), 'fixed']], {}, { class: "form-select", required: true } %>
            </div>
            <div class="col-md-6">
              <label class="form-label"><%= t('bo.pricing.form.discount_value') %> <span class="text-danger">*</span></label>
              <%= f.number_field :discount_value, class: "form-control", placeholder: "0.10", step: 0.01, min: 0, required: true %>
              <small class="text-muted"><%= t('bo.pricing.form.discount_value_hint', symbol: @current_organisation.currency_symbol) %></small>
            </div>
          </div>

          <div class="row g-3 mt-3">
            <div class="col-md-6">
              <div class="form-check form-switch">
                <%= f.check_box :stackable, class: "form-check-input", role: "switch", id: "stackableSwitch" %>
                <label class="form-check-label" for="stackableSwitch">
                  <%= t('bo.pricing.form.stackable_label') %>
                </label>
              </div>
              <small class="text-muted"><%= t('bo.pricing.form.stackable_hint_order') %></small>
            </div>
            <div class="col-md-6">
              <div class="form-check form-switch">
                <%= f.check_box :active, class: "form-check-input", role: "switch", id: "activeSwitch" %>
                <label class="form-check-label" for="activeSwitch">
                  <%= t('bo.pricing.form.active_label') %>
                </label>
              </div>
              <small class="text-muted"><%= t('bo.pricing.form.active_hint') %></small>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="col-md-4">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title mb-4"><%= t('bo.pricing.order_tiers.validity_title') %></h5>
          <p class="text-muted small"><%= t('bo.pricing.form.no_expiration') %></p>

          <div class="mb-3">
            <label class="form-label"><%= t('bo.pricing.form.valid_from') %></label>
            <%= f.date_field :valid_from, class: "form-control" %>
          </div>

          <div>
            <label class="form-label"><%= t('bo.pricing.form.valid_until') %></label>
            <%= f.date_field :valid_until, class: "form-control" %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

</div>

<% end %>
