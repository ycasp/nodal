<% content_for :bo_main_content do %>

<div class="bo-page-header">
  <div class="d-flex justify-content-between align-items-center">
    <div class="d-flex align-items-center gap-3">
      <%= link_to bo_product_variants_path(params[:org_slug], @product), class: "text-muted text-decoration-none" do %>
        <i class="fa-solid fa-arrow-left fa-lg"></i>
      <% end %>

      <div class="rounded bg-secondary d-flex align-items-center justify-content-center text-white" style="width: 60px; height: 60px;">
        <i class="fa-solid fa-cube fa-lg"></i>
      </div>

      <div>
        <h1><%= t('bo.product_variants.new.title') %></h1>
        <span class="text-muted"><%= @product.name %></span>
      </div>
    </div>

    <div class="bo-page-actions">
      <%= link_to bo_product_variants_path(params[:org_slug], @product), class: "btn btn-outline-secondary" do %>
        <%= t('bo.common.actions.cancel') %>
      <% end %>
      <button type="submit" form="variant-form" class="btn btn-primary">
        <i class="fa-solid fa-check"></i> <%= t('bo.product_variants.new.create_variant') %>
      </button>
    </div>
  </div>
</div>

<div class="bo-page-body">

<%= render 'form', variant: @variant %>

</div>

<% end %>
