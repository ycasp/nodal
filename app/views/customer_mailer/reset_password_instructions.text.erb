Hello <%= @resource.contact_name %>,

Someone requested a password reset for your account.

Reset password: <%= edit_customer_password_url(@resource, reset_password_token: @token, org_slug: @resource.organisation.slug) %>

If you didn't request this, ignore this email.
