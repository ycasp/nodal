<% status_class = case @order.status
   when 'completed' then 'bg-success'
   when 'processed' then 'bg-info'
   when 'in_process' then 'bg-warning text-dark'
   else 'bg-secondary'
   end %>
<% actions = capture do %>
  <span class="badge <%= status_class %> fs-6"><%= @order.status.titleize %></span>
<% end %>
<% subtitle = if @order.placed?
     t('storefront.orders.show.placed_on', date: l(@order.placed_at, format: :long))
   else
     t('storefront.orders.show.draft_order')
   end %>
<%= render "storefront/shared/page_header",
  title: t('storefront.orders.show.order_number', number: @order.order_number),
  subtitle: subtitle,
  back_path: orders_path(org_slug: params[:org_slug]),
  back_label: t('storefront.orders.show.back_to_orders'),
  actions: actions %>

<div class="row g-4">
  <div class="col-lg-8">
    <!-- Order Items -->
    <div class="card border-0 shadow-sm mb-4" style="border-radius: 12px;">
      <div class="card-body">
        <h5 class="card-title mb-3"><%= t('storefront.orders.show.order_items') %></h5>

        <table class="table">
          <thead>
            <tr>
              <th><%= t('storefront.orders.show.table.product') %></th>
              <th class="text-center"><%= t('storefront.orders.show.table.quantity') %></th>
              <th class="text-end"><%= t('storefront.checkouts.show.table.price') %></th>
              <th class="text-end"><%= t('storefront.orders.show.table.total') %></th>
            </tr>
          </thead>
          <tbody>
            <% @order.order_items.includes(:product, :product_variant).each do |item| %>
              <tr>
                <td>
                  <div class="d-flex align-items-center">
                    <% if item.effective_photo.present? %>
                      <%= image_tag item.effective_photo, style: "width: 50px; height: 50px; object-fit: cover; border-radius: 6px;", class: "me-3" %>
                    <% else %>
                      <div class="me-3 bg-light d-flex align-items-center justify-content-center" style="width: 50px; height: 50px; border-radius: 6px;">
                        <i class="fa-solid fa-image text-muted"></i>
                      </div>
                    <% end %>
                    <div>
                      <div class="fw-medium"><%= item.product.name %></div>
                      <% if item.product_variant.present? && item.product.variable? %>
                        <small class="text-primary"><%= item.product_variant.sku.presence || item.product_variant.option_values_string %></small><br>
                      <% end %>
                      <small class="text-muted">SKU: <%= item.product_variant&.sku.presence || item.product.sku || '-' %></small>
                    </div>
                  </div>
                </td>
                <td class="text-center"><%= item.quantity %></td>
                <td class="text-end">
                  <% if item.discount_percentage && item.discount_percentage > 0 %>
                    <span class="text-muted text-decoration-line-through small d-block"><%= humanized_money_with_symbol(item.price) %></span>
                    <span class="text-success"><%= humanized_money_with_symbol(item.price - (item.price * item.discount_percentage)) %></span>
                    <span class="badge bg-success-subtle text-success small">-<%= format_discount_percentage(item.discount_percentage) %></span>
                  <% else %>
                    <%= humanized_money_with_symbol(item.price) %>
                  <% end %>
                </td>
                <td class="text-end fw-medium">
                  <% if item.discount_percentage && item.discount_percentage > 0 %>
                    <span class="text-muted text-decoration-line-through small d-block"><%= humanized_money_with_symbol(item.price * item.quantity) %></span>
                    <span class="text-success"><%= humanized_money_with_symbol(item.total_price) %></span>
                  <% else %>
                    <%= humanized_money_with_symbol(item.total_price) %>
                  <% end %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>

        <hr>

        <div class="d-flex justify-content-end">
          <div style="width: 250px;">
            <div class="d-flex justify-content-between mb-2">
              <span class="text-muted"><%= t('storefront.orders.show.summary.subtotal') %></span>
              <span><%= humanized_money_with_symbol(@order.total_amount) %></span>
            </div>
            <% if @order.has_auto_discount_snapshot? %>
              <div class="d-flex justify-content-between mb-2 text-success">
                <span><%= t('storefront.carts.show.order_discount') %> (<%= @order.auto_discount_display %>)</span>
                <span>-<%= humanized_money_with_symbol(@order.auto_order_discount_amount) %></span>
              </div>
            <% end %>
            <% if @order.has_order_discount? %>
              <div class="d-flex justify-content-between mb-2 text-success">
                <span><%= t('storefront.orders.show.summary.discount') %> (<%= @order.order_discount_display %>)</span>
                <span>-<%= humanized_money_with_symbol(@order.order_discount_amount) %></span>
              </div>
            <% end %>
            <% if @order.promo_code_discount_amount_cents.present? && @order.promo_code_discount_amount_cents > 0 %>
              <div class="d-flex justify-content-between mb-2 text-success">
                <span>
                  <i class="fa-solid fa-ticket me-1"></i>
                  <%= t('storefront.promo_codes.label') %>
                  <% if @order.promo_code.present? %>
                    <span class="font-monospace">(<%= @order.promo_code.code %>)</span>
                  <% end %>
                </span>
                <span>-<%= humanized_money_with_symbol(@order.promo_code_discount) %></span>
              </div>
            <% end %>
            <% if @order.tax_amount.present? %>
              <div class="d-flex justify-content-between mb-2">
                <span class="text-muted"><%= t('storefront.orders.show.summary.tax') %></span>
                <span><%= humanized_money_with_symbol(@order.tax_amount) %></span>
              </div>
            <% end %>
            <% if @order.shipping_amount.present? %>
              <div class="d-flex justify-content-between mb-2">
                <span class="text-muted"><%= t('storefront.orders.show.summary.shipping') %></span>
                <span><%= humanized_money_with_symbol(@order.shipping_amount) %></span>
              </div>
            <% end %>
            <div class="d-flex justify-content-between fw-bold border-top pt-2">
              <span><%= t('storefront.orders.show.summary.total') %></span>
              <span><%= humanized_money_with_symbol(@order.grand_total) %></span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <% if @order.notes.present? %>
      <div class="card border-0 shadow-sm" style="border-radius: 12px;">
        <div class="card-body">
          <h5 class="card-title mb-3"><%= t('storefront.orders.show.order_notes') %></h5>
          <p class="mb-0"><%= @order.notes %></p>
        </div>
      </div>
    <% end %>
  </div>

  <div class="col-lg-4">
    <!-- Payment Status -->
    <div class="card border-0 shadow-sm mb-4" style="border-radius: 12px;">
      <div class="card-body">
        <h6 class="text-muted mb-3">
          <i class="fa-solid fa-credit-card me-2"></i><%= t('storefront.orders.show.payment_status') %>
        </h6>
        <% payment_class = case @order.payment_status
           when 'paid' then 'text-success'
           when 'pending' then 'text-warning'
           when 'failed' then 'text-danger'
           else 'text-info'
           end %>
        <span class="fs-5 <%= payment_class %>"><%= @order.payment_status.titleize %></span>
      </div>
    </div>

    <!-- Delivery Method -->
    <% if @order.delivery_method.present? %>
      <div class="card border-0 shadow-sm mb-4" style="border-radius: 12px;">
        <div class="card-body">
          <h6 class="text-muted mb-3">
            <i class="fa-solid fa-<%= @order.pickup? ? 'store' : 'truck' %> me-2"></i><%= t('storefront.orders.show.delivery_method') %>
          </h6>
          <span class="fs-5"><%= @order.pickup? ? t('storefront.orders.show.pickup') : t('storefront.orders.show.delivery') %></span>
        </div>
      </div>
    <% end %>

    <!-- Expected Delivery -->
    <% if @order.receive_on.present? && @order.delivery? %>
      <div class="card border-0 shadow-sm mb-4" style="border-radius: 12px;">
        <div class="card-body">
          <h6 class="text-muted mb-3">
            <i class="fa-solid fa-calendar me-2"></i><%= t('storefront.orders.show.expected_delivery') %>
          </h6>
          <span class="fs-5"><%= @order.receive_on.strftime("%B %d, %Y") %></span>
        </div>
      </div>
    <% end %>

    <!-- Reorder -->
    <div class="mt-4">
      <%= button_to add_to_cart_order_path(org_slug: params[:org_slug], id: @order), method: :post, class: "btn btn-outline-secondary w-100 mb-2" do %>
        <i class="fa-solid fa-cart-plus me-2"></i><%= t('storefront.orders.index.add_to_cart') %>
      <% end %>
      <%= button_to reorder_order_path(org_slug: params[:org_slug], id: @order, confirm: params[:confirm_reorder]), method: :post, class: "btn btn-outline-secondary w-100 mb-2" do %>
        <i class="fa-solid fa-rotate-right me-2"></i><%= t('storefront.orders.index.reorder') %>
      <% end %>
      <%= link_to products_path(org_slug: params[:org_slug]), class: "btn btn-primary w-100" do %>
        <i class="fa-solid fa-shopping-bag me-2"></i><%= t('storefront.orders.show.continue_shopping') %>
      <% end %>
    </div>
  </div>
</div>
